import React from 'react';

import './header.css'

const Header = () => {
  return (
    <header>
         <button className="shadow__btn">catalog</button>
    </header>
  );
};

export default Header;